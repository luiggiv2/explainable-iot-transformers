# Paper figures

Generated by `scripts/11_figures.py` from the `data/` artifacts. Each figure is
saved as `.pdf` (vector, for the manuscript) and `.png` (300 dpi preview).
Color: cividis (sequential, CVD-safe, grayscale-degradable) for magnitude;
Okabe-Ito (CVD-safe) for categorical identity.

| Figure | Supports | Source data | Shows |
|---|---|---|---|
| `fig1_perclass_f1` | RQ1 | baselines_metrics.csv, transformer_metrics.csv | Per-class F1 for the 4 models + macro-F1; DistilBERT competitive, weakest on Web. |
| `fig2_confusion` | RQ1/RQ3 | confusion_distilbert.csv | Row-normalized DistilBERT confusion; DoS↔DDoS confusion, two separated regimes, Web leakage. |
| `fig3_feature_importance` | RQ2/RQ3 | captum_feature_attribution.csv, shap_xgb_feature_importance.csv | Per-class feature importance, DistilBERT-IG vs XGBoost-SHAP (rows normalized to max). `ssh`→BruteForce in both; XGBoost `icmp`→DDoS, `num` everywhere; DistilBERT `proto_num` for low-rate classes. |
| `fig4_robustness` | RQ4 | robustness_records_partial.csv | Prediction stability vs explanation stability (top-5 Jaccard) per class; r=+0.85, volumetric robust / low-rate fragile. |

Regenerate: `.venv/bin/python scripts/11_figures.py`
